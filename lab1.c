#include<stdio.h>
#include<sys/types.h>
#include<unistd.h>
#include<math.h>
void forkexample()
{
    int p,t,r;
    float si,ci;
    printf("enter ptr");
    scanf("%d%d%d",&p,&t,&t);
    si=(p*t*r)/100;
    ci=p*(1+(r/100))math.pow(ci,t)
	printf("child has x =%d \n",++x);	
	else
	printf("parent has x =%d\n",--x);
}
int main()
{
	forkexample();
	return 0;
}
